# Keep the build context small and the image clean. The big offender is
# references/ and child_services/ — gitignored clones, potentially many GB;
# COPYing that into the build would be slow and pointless.
.git
references/
child_services/
data/
*.log
# The scratch outputs and DBs never belong in an image.
**/ass.db
