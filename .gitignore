# Build output — the cool binary `go run .` gives us. Regenerated, not versioned.
/ass
/dist/

# Local overrides of the committed ass.toml, if you keep one.
/ass.local.toml

# ASS's own state: job database and harvested artifact bytes. Regenerated at
# runtime, not versioned.
/data/
